Defination of Recursion = A function is calling itself

key of Recursion = the problem is broken into smaller instances , each of which is solved recursively

Stack Memory = The Function's memory is added to the call Stack 