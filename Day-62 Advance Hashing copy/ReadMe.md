Qus 1 = which built-in javascript object is best suited for implementing the hash map ?
ans = Object, explanation = javascript Object Acts Like a hash map , Where keys are hashed internally for the fast access.

Qus 2 =  what will be the output of the following code ? 
```
let x = 50;

if (x <= 50) {
    if (x === 50) {
       console.log("x is 50");
    } else {
        console.log("x is less than 50");
    }
} else {
    console.log("x is greater than 50");
}
```
ans = x is 50

Qus 3 = which javascript class should you use for a true hash map with non-string keys?
ans = map 

Qus 4 = Guess the output 
```
const obj = {};
const key1 = {};
const key2 = {};

obj[key1] = "Hello";

console.log(obj[key2]);
```
ans = undefined

Qus 5 = which statement about javascript map is true ?
ans = Maintain the intersection order 